import React from 'react';
import {View, Text} from 'react-native';

const index = () => {
  return (
    <View>
      <Text style={{fontSize: 24, alignSelf: 'center'}}>Search Patient, fill the form, enable scanner</Text>
    </View>
  );
};

export default index;